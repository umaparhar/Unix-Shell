# Unix-Shell
Implementation of a command line interpreter(CLI)/shell. The shell operates in a basic way: when you type in a command (in response to a prompt), the shell creates a child process that executes the command you entered and then prompts for more user input when it has finished.
